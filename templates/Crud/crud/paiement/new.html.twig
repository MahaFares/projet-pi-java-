{% extends 'base.html.twig' %}

{% block title %}Nouveau paiement | Admin{% endblock %}

{% block body %}
<section class="breadcrumb_area">
    <div class="container">
        <div class="page-cover text-center">
            <h2 class="page-cover-tittle">Nouveau paiement</h2>
            <ol class="breadcrumb">
                <li><a href="{{ path('app_home') }}">Accueil</a></li>
                <li><a href="{{ path('app_paiement_index') }}">Paiements</a></li>
                <li class="active">Nouveau</li>
            </ol>
        </div>
    </div>
</section>

<section class="accomodation_area section_gap">
    <div class="container">
        {% for message in app.flashes('error') %}
            <div class="alert alert-danger">{{ message }}</div>
        {% endfor %}

        {{ form_start(form) }}
        {{ form_row(form.commande) }}
        {{ form_row(form.montant) }}
        {{ form_row(form.methodePaiement) }}
        {{ form_row(form.statutPaiement) }}
        {{ form_row(form.datePaiement) }}
        {{ form_row(form.referencePaiement) }}
        <button type="submit" class="btn theme_btn button_hover">Enregistrer</button>
        <a href="{{ path('app_paiement_index') }}" class="btn btn-secondary">Annuler</a>
        {{ form_end(form) }}
    </div>
</section>
{% endblock %}
