<form method="post" action="{{ path('app_activity_category_delete', {'id': activity_category.id}) }}" onsubmit="return confirm('Supprimer cette catégorie ?');" class="d-inline">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activity_category.id) }}">
    <button type="submit" class="modern-btn btn-delete" style="background:#fef2f2;color:#dc2626;border:2px solid #fecaca;">
        <i class="bi bi-trash"></i> Supprimer
    </button>
</form>
