<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Activité - {{ activity.title }}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'DejaVu Sans', Arial, sans-serif;
            font-size: 11pt;
            line-height: 1.6;
            color: #2d3436;
            background: #f8f9fa;
        }

        /* Tunisia Header with Nature Theme */
        .header {
            background: linear-gradient(135deg, #00b894 0%, #00cec9 50%, #74b9ff 100%);
            color: white;
            padding: 40px 30px;
            position: relative;
            overflow: hidden;
        }

        .header::before {
            content: '🌴';
            position: absolute;
            font-size: 120pt;
            opacity: 0.1;
            right: -20px;
            top: -30px;
        }

        .header::after {
            content: '🌳';
            position: absolute;
            font-size: 80pt;
            opacity: 0.1;
            left: 20px;
            bottom: -20px;
        }

        .logo-section {
            display: table;
            width: 100%;
            margin-bottom: 20px;
        }

        .logo-left {
            display: table-cell;
            width: 70%;
        }

        .logo-right {
            display: table-cell;
            width: 30%;
            text-align: right;
            font-size: 9pt;
            opacity: 0.9;
        }

        .header h1 {
            font-size: 26pt;
            margin-bottom: 10px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.2);
        }

        .header .subtitle {
            font-size: 11pt;
            opacity: 0.95;
            font-weight: 300;
        }

        .location-badge {
            display: inline-block;
            background: rgba(255,255,255,0.3);
            padding: 8px 16px;
            border-radius: 25px;
            margin-top: 10px;
            font-size: 10pt;
            font-weight: bold;
        }

        /* Main Content Container */
        .content {
            padding: 30px;
            background: white;
        }

        /* Image with Nature Frame */
        .image-container {
            text-align: center;
            margin: 0 -30px 30px -30px;
            position: relative;
            background: linear-gradient(135deg, #dfe6e9 0%, #b2bec3 100%);
            padding: 20px;
        }

        .image-container img {
            max-width: 100%;
            height: auto;
            border-radius: 15px;
            box-shadow: 0 8px 20px rgba(0,0,0,0.15);
            border: 5px solid white;
        }

        .image-caption {
            position: absolute;
            bottom: 30px;
            left: 50px;
            background: rgba(0,184,148,0.9);
            color: white;
            padding: 10px 20px;
            border-radius: 8px;
            font-weight: bold;
            font-size: 10pt;
        }

        /* Info Cards with Nature Colors */
        .info-cards {
            display: table;
            width: 100%;
            margin-bottom: 25px;
            border-spacing: 15px;
        }

        .info-card {
            display: table-cell;
            width: 25%;
            background: linear-gradient(135deg, #00b894 0%, #00cec9 100%);
            color: white;
            padding: 20px;
            border-radius: 12px;
            text-align: center;
            box-shadow: 0 4px 10px rgba(0,184,148,0.3);
        }

        .info-card-icon {
            font-size: 28pt;
            margin-bottom: 10px;
            display: block;
        }

        .info-card-label {
            font-size: 8pt;
            opacity: 0.9;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 5px;
        }

        .info-card-value {
            font-size: 16pt;
            font-weight: bold;
        }

        /* Section Styling */
        .section {
            margin-bottom: 25px;
            background: #f8f9fa;
            padding: 20px;
            border-radius: 12px;
            border-left: 5px solid #00b894;
        }

        .section-title {
            color: #00b894;
            font-size: 14pt;
            margin-bottom: 15px;
            font-weight: bold;
            display: flex;
            align-items: center;
        }

        .section-title::before {
            content: '🌿';
            margin-right: 10px;
            font-size: 16pt;
        }

        /* Details Grid */
        .details-grid {
            display: table;
            width: 100%;
            background: white;
            border-radius: 8px;
            padding: 15px;
        }

        .detail-row {
            display: table-row;
        }

        .detail-label {
            display: table-cell;
            font-weight: bold;
            padding: 10px 15px;
            width: 35%;
            color: #636e72;
            border-bottom: 1px solid #f0f0f0;
        }

        .detail-value {
            display: table-cell;
            padding: 10px 15px;
            color: #2d3436;
            border-bottom: 1px solid #f0f0f0;
        }

        .detail-row:last-child .detail-label,
        .detail-row:last-child .detail-value {
            border-bottom: none;
        }

        /* Description Box */
        .description-box {
            background: white;
            padding: 20px;
            border-radius: 8px;
            line-height: 1.8;
            text-align: justify;
            border: 2px dashed #00b894;
        }

        /* Category & Status Badges */
        .badge {
            display: inline-block;
            padding: 6px 16px;
            border-radius: 20px;
            font-size: 9pt;
            font-weight: bold;
            margin: 3px;
        }

        .badge-active {
            background: #d4edda;
            color: #155724;
            border: 2px solid #28a745;
        }

        .badge-inactive {
            background: #f8d7da;
            color: #721c24;
            border: 2px solid #dc3545;
        }

        .badge-category {
            background: #e0f7fa;
            color: #006064;
            border: 2px solid #00acc1;
        }

        /* Price Highlight */
        .price-box {
            background: linear-gradient(135deg, #fdcb6e 0%, #f39c12 100%);
            color: white;
            padding: 20px;
            border-radius: 12px;
            text-align: center;
            margin: 20px 0;
            box-shadow: 0 4px 15px rgba(243,156,18,0.3);
        }

        .price-label {
            font-size: 10pt;
            opacity: 0.9;
            text-transform: uppercase;
        }

        .price-value {
            font-size: 32pt;
            font-weight: bold;
            margin: 10px 0;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.2);
        }

        .price-note {
            font-size: 8pt;
            opacity: 0.8;
        }

        /* Location Box with Tunisia Style */
        .location-box {
            background: linear-gradient(135deg, #fff3cd 0%, #ffeaa7 100%);
            border-left: 5px solid #fdcb6e;
            padding: 20px;
            margin: 20px 0;
            border-radius: 10px;
            display: table;
            width: 100%;
        }

        .location-icon {
            display: table-cell;
            width: 60px;
            font-size: 32pt;
            vertical-align: middle;
        }

        .location-text {
            display: table-cell;
            vertical-align: middle;
            padding-left: 15px;
        }

        .location-title {
            font-weight: bold;
            font-size: 11pt;
            color: #d63031;
            margin-bottom: 5px;
        }

        .location-address {
            font-size: 10pt;
            color: #636e72;
        }

        /* Guide Box */
        .guide-box {
            background: linear-gradient(135deg, #dfe6e9 0%, #b2bec3 100%);
            border-left: 5px solid #74b9ff;
            padding: 20px;
            margin: 20px 0;
            border-radius: 10px;
        }

        .guide-header {
            font-weight: bold;
            font-size: 12pt;
            color: #0984e3;
            margin-bottom: 10px;
        }

        .guide-details {
            font-size: 10pt;
            color: #2d3436;
            line-height: 1.8;
        }

        /* Schedule Table with Nature Theme */
        .schedule-table {
            width: 100%;
            border-collapse: collapse;
            background: white;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        }

        .schedule-table th {
            background: linear-gradient(135deg, #00b894 0%, #00cec9 100%);
            color: white;
            padding: 15px;
            text-align: left;
            font-size: 10pt;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .schedule-table td {
            padding: 12px 15px;
            border-bottom: 1px solid #ecf0f1;
            font-size: 10pt;
        }

        .schedule-table tr:last-child td {
            border-bottom: none;
        }

        .schedule-table tr:nth-child(even) {
            background: #f8f9fa;
        }

        .schedule-table tr:hover {
            background: #e8f8f5;
        }

        .spots-available {
            color: #27ae60;
            font-weight: bold;
        }

        .spots-full {
            color: #e74c3c;
            font-weight: bold;
        }

        /* Footer with Tunisia Branding */
        .footer {
            margin-top: 40px;
            padding: 25px 30px;
            background: linear-gradient(135deg, #2d3436 0%, #636e72 100%);
            color: white;
            text-align: center;
            border-radius: 0 0 12px 12px;
        }

        .footer-logo {
            font-size: 18pt;
            font-weight: bold;
            margin-bottom: 10px;
            color: #00b894;
        }

        .footer-text {
            font-size: 9pt;
            opacity: 0.8;
            line-height: 1.6;
        }

        .footer-icons {
            margin-top: 15px;
            font-size: 16pt;
        }

        /* Tunisia Decorative Elements */
        .tunisia-pattern {
            border-top: 3px solid #00b894;
            border-bottom: 3px solid #00b894;
            padding: 15px 0;
            margin: 20px 0;
            text-align: center;
            font-style: italic;
            color: #636e72;
            background: repeating-linear-gradient(
                    45deg,
                    transparent,
                    transparent 10px,
                    rgba(0,184,148,0.05) 10px,
                    rgba(0,184,148,0.05) 20px
            );
        }

        /* Page Break */
        .page-break {
            page-break-after: always;
        }
    </style>
</head>
<body>

<!-- Header Section -->
<div class="header">
    <div class="logo-section">
        <div class="logo-left">
            <div style="font-size: 16pt; font-weight: bold; margin-bottom: 5px;">🌿 Activités Nature Tunisie</div>
            <div style="font-size: 9pt; opacity: 0.9;">Découvrez la beauté du Grand Tunis et du Nord</div>
        </div>
        <div class="logo-right">
            <div>📅 {{ generatedAt|date('d/m/Y') }}</div>
            <div>🕐 {{ generatedAt|date('H:i') }}</div>
        </div>
    </div>

    <h1>{{ activity.title }}</h1>
    <div class="subtitle">Activité #{{ activity.id }} - Réservation disponible</div>
    <span class="location-badge">📍 {{ activity.location }}</span>
</div>

<div class="content">
    <!-- Main Image -->
    {% if activity.image %}
        <div class="image-container">
            <img src="{{ absolute_url(asset(activity.image)) }}" alt="{{ activity.title }}">
            <div class="image-caption">🌳 Activité Nature en Tunisie</div>
        </div>
    {% endif %}

    <!-- Quick Info Cards -->
    <div class="info-cards">
        <div class="info-card">
            <span class="info-card-icon">💰</span>
            <div class="info-card-label">Prix</div>
            <div class="info-card-value">{{ activity.price }} DT</div>
        </div>
        <div class="info-card">
            <span class="info-card-icon">⏱️</span>
            <div class="info-card-label">Durée</div>
            <div class="info-card-value">{{ (activity.durationMinutes / 60)|round(1) }}h</div>
        </div>
        <div class="info-card">
            <span class="info-card-icon">👥</span>
            <div class="info-card-label">Participants</div>
            <div class="info-card-value">{{ activity.maxParticipants ?? '∞' }}</div>
        </div>
        <div class="info-card">
            <span class="info-card-icon">✅</span>
            <div class="info-card-label">Statut</div>
            <div class="info-card-value" style="font-size: 11pt;">{{ activity.isActive ? 'Actif' : 'Inactif' }}</div>
        </div>
    </div>

    <!-- Tunisia Pattern Separator -->
    <div class="tunisia-pattern">
        🌴 Explorez les merveilles naturelles de la Tunisie 🌴
    </div>

    <!-- Description Section -->
    <div class="section">
        <div class="section-title">Description de l'activité</div>
        <div class="description-box">
            {{ activity.description|nl2br }}
        </div>
    </div>

    <!-- Details Section -->
    <div class="section">
        <div class="section-title">Informations détaillées</div>
        <div class="details-grid">
            <div class="detail-row">
                <div class="detail-label">🆔 Référence</div>
                <div class="detail-value">#{{ activity.id }}</div>
            </div>
            <div class="detail-row">
                <div class="detail-label">📂 Catégorie</div>
                <div class="detail-value">
                    {% if activity.category %}
                        <span class="badge badge-category">{{ activity.category.name }}</span>
                    {% else %}
                        Non catégorisé
                    {% endif %}
                </div>
            </div>
            <div class="detail-row">
                <div class="detail-label">⏱️ Durée totale</div>
                <div class="detail-value">{{ activity.durationMinutes }} minutes</div>
            </div>
            <div class="detail-row">
                <div class="detail-label">👥 Nombre maximum</div>
                <div class="detail-value">{{ activity.maxParticipants ?? 'Illimité' }} participants</div>
            </div>
            <div class="detail-row">
                <div class="detail-label">✅ Disponibilité</div>
                <div class="detail-value">
                    <span class="badge {% if activity.isActive %}badge-active{% else %}badge-inactive{% endif %}">
                        {{ activity.isActive ? '✓ Disponible' : '✗ Indisponible' }}
                    </span>
                </div>
            </div>
        </div>
    </div>

    <!-- Location -->
    <div class="location-box">
        <div class="location-icon">📍</div>
        <div class="location-text">
            <div class="location-title">Lieu de l'activité</div>
            <div class="location-address">{{ activity.location }} - Grand Tunis / Nord Tunisie</div>
        </div>
    </div>

    <!-- Guide Information -->
    {% if activity.guide %}
        <div class="guide-box">
            <div class="guide-header">👤 Votre Guide Professionnel</div>
            <div class="guide-details">
                <strong>Nom :</strong> {{ activity.guide.firstName }} {{ activity.guide.lastName }}<br>
                {% if activity.guide.email %}
                    <strong>📧 Email :</strong> {{ activity.guide.email }}<br>
                {% endif %}
                {% if activity.guide.phone %}
                    <strong>📞 Téléphone :</strong> {{ activity.guide.phone }}
                {% endif %}
            </div>
        </div>
    {% endif %}

    <!-- Price Highlight -->
    <div class="price-box">
        <div class="price-label">💎 Tarif de l'activité</div>
        <div class="price-value">{{ activity.price }} DT</div>
        <div class="price-note">Prix par personne - Paiement sur place accepté</div>
    </div>

    <!-- Schedules -->
    {% if activity.schedules|length > 0 %}
        <div class="section">
            <div class="section-title">Horaires et disponibilités</div>
            <table class="schedule-table">
                <thead>
                <tr>
                    <th>📅 Date de début</th>
                    <th>🕐 Date de fin</th>
                    <th>🎫 Places disponibles</th>
                </tr>
                </thead>
                <tbody>
                {% for schedule in activity.schedules %}
                    <tr>
                        <td>{{ schedule.startAt|date('d/m/Y à H:i') }}</td>
                        <td>{{ schedule.endAt|date('d/m/Y à H:i') }}</td>
                        <td>
                            {% if schedule.availableSpots > 0 %}
                                <span class="spots-available">✅ {{ schedule.availableSpots }} places</span>
                            {% else %}
                                <span class="spots-full">❌ Complet</span>
                            {% endif %}
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    {% endif %}

    <!-- Tunisia Pattern Separator -->
    <div class="tunisia-pattern">
        🌊 Une expérience inoubliable dans le nord de la Tunisie 🏔️
    </div>

</div>

<!-- Footer -->
<div class="footer">
    <div class="footer-logo">🌿 Activités Nature Tunisie 🌿</div>
    <div class="footer-text">
        Document officiel généré le {{ generatedAt|date('d/m/Y à H:i:s') }}<br>
        Grand Tunis & Nord Tunisie - Votre destination nature<br>
        Découvrez la beauté naturelle de la Tunisie avec nos activités guidées
    </div>
    <div class="footer-icons">
        🌳 🌴 🌊 🏔️ 🌿 ☀️
    </div>
</div>

</body>
</html>
