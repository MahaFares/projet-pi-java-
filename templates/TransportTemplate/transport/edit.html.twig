{% extends 'BackOffice/base.html.twig' %}

{% block title %}Modifier le transport - Admin{% endblock %}

{% block body %}
<div class="container py-4">
    {{ include('admin_nav.html.twig') }}
    <h1>Modifier le transport</h1>
    {{ include('TransportTemplate/transport/_form.html.twig', {'button_label': 'Enregistrer'}) }}
    <a href="{{ path('app_transport_index') }}" class="btn btn-secondary mt-2">Retour à la liste</a>
    {{ include('TransportTemplate/transport/_delete_form.html.twig') }}
</div>
{% endblock %}
