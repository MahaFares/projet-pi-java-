{% extends 'BackOffice/base.html.twig' %}

{% block title %}Modifier {{ produit.nom }} - Admin{% endblock %}

{% block body %}
<div class="container py-4">
    {{ include('admin_nav.html.twig') }}
    <h1>Modifier le produit : {{ produit.nom }}</h1>
    {{ include('ProductTemplate/produit/_form.html.twig') }}
    <a href="{{ path('app_produit_index') }}" class="btn btn-secondary mt-2">Retour à la liste</a>
</div>
{% endblock %}
