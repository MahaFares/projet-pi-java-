{% extends 'BackOffice/base.html.twig' %}

{% block title %}Nouvelle Catégorie | EcoTrip Admin{% endblock %}
{% block page_title %}Ajouter une Catégorie{% endblock %}

{% block body %}
<div class="container-fluid py-4">
    <div class="row justify-content-center">
        <div class="col-xxl-6 col-md-8">
            <div class="d-flex align-items-center justify-content-between mb-4">
                <div>
                    <h2 class="h3 fw-bold text-dark mb-1">Créer une catégorie</h2>
                    <p class="text-muted">Définissez une nouvelle catégorie d'hébergement pour la plateforme.</p>
                </div>
                <a href="{{ path('app_categorie_hebergement_index') }}" class="btn btn-outline-secondary rounded-pill px-4">
                    <i class="bi bi-arrow-left me-2"></i>Retour
                </a>
            </div>

            {{ include('HebergementTemplate/categorie_hebergement/_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
