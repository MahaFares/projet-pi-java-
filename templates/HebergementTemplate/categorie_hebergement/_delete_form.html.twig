<form method="post" action="{{ path('app_categorie_hebergement_delete', {'id': categorie_hebergement.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette catégorie ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_hebergement.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
