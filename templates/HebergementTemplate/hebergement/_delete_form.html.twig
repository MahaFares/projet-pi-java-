<form method="post" action="{{ path('app_hebergement_delete', {'id': hebergement.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet hébergement ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hebergement.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
