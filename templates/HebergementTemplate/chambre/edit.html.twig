{% extends 'base.html.twig' %}

{% block title %}Modifier une chambre - Admin{% endblock %}

{% block body %}
<div class="container py-4">
    {{ include('admin_nav.html.twig') }}
    <h1>Modifier une chambre</h1>
    {{ include('HebergementTemplate/chambre/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    <a href="{{ path('app_chambre_index') }}" class="btn btn-secondary mt-2">Retour à la liste</a>
    <div class="mt-3">
        {{ include('HebergementTemplate/chambre/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
