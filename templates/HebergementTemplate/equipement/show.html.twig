{% extends 'base.html.twig' %}

{% block title %}Équipement - Admin{% endblock %}

{% block body %}
<div class="container py-4">
    {{ include('admin_nav.html.twig') }}
    <h1>Détails de l'équipement</h1>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ equipement.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ equipement.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ equipement.description }}</td>
            </tr>
        </tbody>
    </table>

    <div class="mt-3">
        <a href="{{ path('app_equipement_index') }}" class="btn btn-secondary">Retour à la liste</a>
        <a href="{{ path('app_equipement_edit', {'id': equipement.id}) }}" class="btn btn-primary ms-2">Modifier</a>
    </div>

<<<<<<< HEAD
    <a href="{{ path('app_equipement_edit', {'id': equipement.id}) }}">edit</a>

    {{ include('HebergementTemplate/equipement/_delete_form.html.twig') }}
=======
    <div class="mt-3">
        {{ include('HebergementTemplate/equipement/_delete_form.html.twig') }}
    </div>
</div>
>>>>>>> d1fec4b (temp)
{% endblock %}
