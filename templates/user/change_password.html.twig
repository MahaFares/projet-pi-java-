{% extends 'base.html.twig' %}

{% block title %}Changer le mot de passe | Eco Trip{% endblock %}

{% block body %}
<section class="breadcrumb_area">
    <div class="overlay bg-parallax"></div>
    <div class="container">
        <div class="page-cover text-center">
            <h2 class="page-cover-tittle">Changer le mot de passe</h2>
            <ol class="breadcrumb">
                <li><a href="{{ path('app_home') }}">Home</a></li>
                <li><a href="{{ path('app_my_account') }}">Mon compte</a></li>
                <li class="active">Mot de passe</li>
            </ol>
        </div>
    </div>
</section>

<section class="accomodation_area section_gap">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                {{ form_start(form) }}
                    {{ form_row(form.currentPassword) }}
                    {{ form_row(form.newPassword.first) }}
                    {{ form_row(form.newPassword.second) }}

                    <button class="btn theme_btn button_hover w-100">
                        Mettre à jour le mot de passe
                    </button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
</section>
{% endblock %}
