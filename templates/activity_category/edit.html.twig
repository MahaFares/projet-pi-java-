{% extends 'base.html.twig' %}

{% block title %}Modifier la catégorie - Admin{% endblock %}

{% block body %}
<div class="container py-4">
    {{ include('admin_nav.html.twig') }}
    <h1>Modifier la catégorie</h1>
    {{ include('activity_category/_form.html.twig', {'button_label': 'Enregistrer'}) }}
    <a href="{{ path('app_activity_category_index') }}" class="btn btn-secondary mt-2">Retour à la liste</a>
    {{ include('activity_category/_delete_form.html.twig') }}
</div>
{% endblock %}
