<!DOCTYPE html>

<title>Edit ActivityCategory</title>

{% block body %}
    <h1>Edit ActivityCategory</h1>

    {{ include('activity_category/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_activity_category_index') }}">back to list</a>

    {{ include('activity_category/_delete_form.html.twig') }}
{% endblock %}
